import React from 'react';
import { Style } from './style'

const LoteDetalhe = ({ text }) => {

  return (

    <Style>
      <h4>{text}</h4>
      <button>Click</button>
    </Style>
  )
}

export default LoteDetalhe;